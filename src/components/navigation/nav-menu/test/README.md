# NavMenu Tests

Tests for `NavMenu` component.

- `NavMenu.test.tsx` — verifies avatar initials, opens menu on click, shows links and Sign out button when authenticated, and calls logout handler.

Run:

```bash
npx vitest src/components/navigation/nav-menu/test
```
