# App Configuration
VITE_APP_VERSION=2.27.0
VITE_ROOT_URL=http://localhost:3002

# GraphQL API URL
# Used by apolloClient.ts if VITE_GRAPHQL_URL env var is set
VITE_GRAPHQL_URL=http://localhost:3002/graphql

# ==============================================================================
# SETUP INSTRUCTIONS
# ==============================================================================
# 
# For local development:
#   1. Copy this file: cp .env.example .env.local
#   2. Update VITE_ROOT_URL and VITE_GRAPHQL_URL to your local backend
#   3. The .env.local file is gitignored - never commit it!
#
# For production:
#   - Set these as environment variables in your deployment platform
#   - Or use .env.production.local (also gitignored)
#
# Vite Environment Variable Priority (highest to lowest):
#   .env.[mode].local  (e.g., .env.production.local)
#   .env.[mode]        (e.g., .env.production)
#   .env.local
#   .env
# ==============================================================================
