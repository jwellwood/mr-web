#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

npm run codegen
# Stage generated files so the commit includes the latest outputs.
git add -A
if [ -n "$(git status --porcelain)" ]; then
  echo "Note: codegen ran and changes were staged. Commit will include generated files."
fi
